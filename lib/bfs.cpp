#include <iostream>
#include <queue>
using namespace std;

int direct[4][2] = {{0, 1}, {1, 0}, {0, -1}, {-1, 0}};

void bfs() {
    queue q;

    while (not q.empty()) {
        int cur = q.pop();
        int next;
        q.push(next);
    }
}